/**
 * A static class that defines the default root path of an Helios server instance.
 */
export class Root {

    /**
     * The default root path of an Helios server instance.
     */
    public static readonly DEFAULT_PATH: string = '/asteria';
}